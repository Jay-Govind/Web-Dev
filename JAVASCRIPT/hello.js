console.log("Hello World")
/* 
    What happened when we run any JS-file?
    - The JS engine reads the file and executes the code line by line.
    - If there is an error in the code, the engine stops executing the code and shows the error.
    - If there is no error, the engine executes the code and shows the output.
    - The output can be shown in the terminal or in the browser console.

    How to run a JS file?
    - Open the terminal.
    - Go to the path where the file is located.
    - Run the command "node hello.js" in the terminal.

    
*/