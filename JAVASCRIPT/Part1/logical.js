// && -> and
// || -> or
// ! -> not (reverse)

let isLoggedIn = true;
let isPaidMember = false;

console.log(isLoggedIn && isPaidMember); // false

let isEmailusername = true;
let isGoogleuser = false;

console.log(isEmailusername || isGoogleuser); // true